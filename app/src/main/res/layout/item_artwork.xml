<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:orientation="horizontal"
    android:background="@color/grey_near_black"
    android:gravity="center_vertical">

    <!-- Artwork Image (Left) -->
    <ImageView
        android:id="@+id/artwork_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        android:src="@drawable/placeholder_image"
        android:contentDescription="Artwork image"/>

    <!-- Title (Right, Wraps Automatically) -->
    <TextView
        android:id="@+id/artwork_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingStart="12dp"
        android:text="The Red Kerchief, c.1868-73. Claude Monet (French, 1840-1926)."
        android:textSize="16sp"
        android:textColor="@color/text_white"
        android:maxLines="3"
        android:ellipsize="end"
        android:gravity="center_vertical"/>
</LinearLayout>

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@android:color/black"
    android:padding="16dp"
    android:gravity="center_vertical">

    <!-- Text Layout (Left Side) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- "The" on the left -->
            <TextView
                android:id="@+id/text_the"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="The"
                android:textSize="18sp"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:layout_marginEnd="8dp"/>

            <!-- "Exhibition" next to "The", bigger -->
            <TextView
                android:id="@+id/text_exhibition"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Exhibition"
                android:textSize="24sp"
                android:textColor="@android:color/white"/>
        </LinearLayout>

        <!-- "Curator" below "Exhibition", left-aligned with it -->
        <TextView
            android:id="@+id/text_curator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Curator"
            android:textSize="24sp"
            android:textColor="@android:color/white"
            android:layout_marginTop="-4dp"
            android:layout_marginStart="40dp"/>
    </LinearLayout>

    <!-- Slogan on the Right -->
    <TextView
        android:id="@+id/text_slogan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Be your own curator.\nCreate your collection."
        android:textSize="15sp"
        android:textColor="@android:color/white"
        android:textAlignment="viewEnd"
        android:gravity="end"
        android:lineSpacingExtra="6dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"/>
</LinearLayout>
